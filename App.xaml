<Application x:Class="KOYA_APP.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <FontFamily x:Key="MainFont">Audiowide, Segoe UI Semibold</FontFamily>
            <FontFamily x:Key="IconFont">Segoe MDL2 Assets</FontFamily>

            <!-- Achromatic Cyber Colors -->
            <SolidColorBrush x:Key="CyberWhite" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="CyberGrey" Color="#888888"/>
            <SolidColorBrush x:Key="CyberDarkGrey" Color="#444444"/>
            <SolidColorBrush x:Key="CyberBlack" Color="#050505"/>
            
            <Style x:Key="GlassButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#0DFFFFFF"/>
                <Setter Property="Foreground" Value="#AAFFFFFF"/>
                <Setter Property="BorderBrush" Value="#44FFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Width" Value="110"/>
                <Setter Property="Height" Value="110"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <ScaleTransform x:Name="mainScale" ScaleX="1" ScaleY="1"/>
                                </Grid.RenderTransform>
                                
                                <!-- Outer Glow (White) -->
                                <Border x:Name="glow" CornerRadius="0" Opacity="0" Background="White">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="White" Opacity="0.5"/>
                                    </Border.Effect>
                                </Border>

                                <!-- Main Body (Sharp Corners) -->
                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        CornerRadius="0" ClipToBounds="True">
                                    <Grid>
                                        <Rectangle x:Name="sweep" Fill="White" Opacity="0" Width="40" Height="200" HorizontalAlignment="Left" Margin="-100,0,0,0">
                                            <Rectangle.RenderTransform>
                                                <RotateTransform Angle="25"/>
                                            </Rectangle.RenderTransform>
                                            <Rectangle.Effect>
                                                <BlurEffect Radius="30"/>
                                            </Rectangle.Effect>
                                        </Rectangle>

                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ContentPresenter.Resources>
                                                <Style TargetType="Image">
                                                    <Setter Property="Margin" Value="32"/>
                                                    <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
                                                </Style>
                                            </ContentPresenter.Resources>
                                        </ContentPresenter>
                                    </Grid>
                                </Border>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleX" To="1.05" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleY" To="1.05" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0.2" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#22FFFFFF" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)" To="White" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#0DFFFFFF" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)" To="#44FFFFFF" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleX" To="0.95" Duration="0:0:0.1"/>
                                                <DoubleAnimation Storyboard.TargetName="mainScale" Storyboard.TargetProperty="ScaleY" To="0.95" Duration="0:0:0.1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="KnobButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#0AFFFFFF"/>
                <Setter Property="Foreground" Value="#77FFFFFF"/>
                <Setter Property="BorderBrush" Value="#44FFFFFF"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="knobGrid" RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Grid.RenderTransform>
                                
                                <!-- Orbital Ring (Independent container) -->
                                <Grid x:Name="orbitalContainer" RenderTransformOrigin="0.5,0.5">
                                    <Grid.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                    </Grid.RenderTransform>
                                    <Ellipse x:Name="orbital" Margin="-5" Stroke="White" StrokeThickness="1" StrokeDashArray="4 2" Opacity="0.1"/>
                                </Grid>
                                
                                <!-- Glow (White) -->
                                <Ellipse x:Name="glow" Fill="White" Opacity="0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="White" Opacity="0.5"/>
                                    </Ellipse.Effect>
                                </Ellipse>

                                <!-- Main Knob -->
                                <Ellipse x:Name="el" Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                
                                <!-- Pointer Marker -->
                                <Rectangle Fill="White" Width="2" Height="10" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="knobGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="knobGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.3"/>
                                                
                                                <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0.2" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="el" Storyboard.TargetProperty="(Shape.Stroke).(SolidColorBrush.Color)" To="White" Duration="0:0:0.3"/>
                                                
                                                <!-- Spin ONLY the orbital container -->
                                                <DoubleAnimation Storyboard.TargetName="orbitalContainer" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="360" Duration="0:0:15" RepeatBehavior="Forever"/>
                                                <DoubleAnimation Storyboard.TargetName="orbital" Storyboard.TargetProperty="Opacity" To="0.6" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="knobGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="knobGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                                                <ColorAnimation Storyboard.TargetName="el" Storyboard.TargetProperty="(Shape.Stroke).(SolidColorBrush.Color)" To="#44FFFFFF" Duration="0:0:0.3"/>
                                                <DoubleAnimation Storyboard.TargetName="orbital" Storyboard.TargetProperty="Opacity" To="0.1" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ActionListBoxStyle" TargetType="ListBox">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Grid>
                                            <Border x:Name="itemBorder" Background="{TemplateBinding Background}" Padding="15" BorderThickness="0,0,0,1" BorderBrush="#11FFFFFF">
                                                <ContentPresenter />
                                            </Border>
                                            <Rectangle x:Name="marker" HorizontalAlignment="Left" Width="0" Fill="White" Margin="0,10"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="itemBorder" Property="Background" Value="#08FFFFFF"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="itemBorder" Property="Background" Value="#1AFFFFFF"/>
                                                <Setter TargetName="marker" Property="Width" Value="4"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
